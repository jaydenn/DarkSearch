double BgRate(detectorSpecs D, double Er_min, double Er_max);         //detector background integrated between two energies in events/kg/day

int newDetector(detector *det, char *name, double exp, int ndet);
